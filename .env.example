# Copy this file to .env and set your real values.
# Do not commit .env — it is in .gitignore.

# Required: your OpenAI API key
OPENAI_API_KEY=your-key-here

# Optional: judge model (default: gpt-4o)
# Examples: gpt-4o, gpt-4o-mini, gpt-4-turbo
JUDGE_MODEL=gpt-4o

# Optional: timeout in seconds for each OpenAI call (default: 60)
OPENAI_TIMEOUT=60

# Optional: CORS origins, comma-separated (default: * allows all)
# Example: https://app.example.com,https://admin.example.com
# CORS_ORIGINS=*

# Optional: rate limit per IP (default: 30/minute). Examples: 10/minute, 100/hour
# RATE_LIMIT=30/minute

# Optional: set to 1, true, or yes to expose full error messages in 500/504 responses
# DEBUG=0
